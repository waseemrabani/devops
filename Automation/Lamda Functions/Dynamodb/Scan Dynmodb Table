import boto3
from boto3.dynamodb.conditions import Key

client = boto3.resource('dynamodb')
def lambda_handler(event, context):
    table = client.Table("VPN")
    #VPN = table.scan()
    #print(VPN)
    response = table.scan()
    result = response['Items']
    print(result)
  
    
